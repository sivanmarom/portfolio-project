version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      # שימי לב ל-?sslmode=require בסוף!
      DATABASE_URL: "postgresql+psycopg2://visits_doh2_user:iLtRcWYAwBFpMhcsOSLYkqKLlXX4LbMS@dpg-d2asrl49c44c7382jbug-a/visits_doh2?sslmode=require"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:5000/api/hello || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped